"""Counts collected into bins or cells rather than plotted per observation.

One of the mark families #524 groups the package into. Nothing is
re-exported here: call sites name the module they need, and the public
API is re-exported from `dataviz/__init__.mojo`, unchanged.
"""
